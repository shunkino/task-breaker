[project]
name = "task-breaker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "github-copilot-sdk",
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy",
  "jinja2",
  "python-multipart",
  "httpx",
  "typer",
  "apscheduler",
  "pydantic-settings",
]

[tool.uv]
package = false
